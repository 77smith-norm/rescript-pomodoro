import reactHooks from "eslint-plugin-react-hooks";
import reactCompiler from "eslint-plugin-react-compiler";

// ReScript .res.jsx files are compiled output - skip ESLint parsing
// The ReScript compiler handles type checking and syntax errors
export default [];
