// Placeholder — agent will replace this with the full implementation
@react.component
let make = () => {
  <div className="min-h-screen flex items-center justify-center">
    {React.string("rescript-pomodoro loading...")}
  </div>
}
